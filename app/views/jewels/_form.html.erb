<%= form_for @jewel, url: {controller: "jewels", action: "create"} do |f| %>
	<%= f.text_field :body %>
	<%= hidden_field_tag '[jewel][jot_id]', @jot.id unless @jot.nil? %>
	<%= hidden_field_tag '[jewel][id]', @jewel.id unless @jewel.id.nil? %>
	<br>
	<%= f.submit %>
<% end %>
