<% @jots.each do |jot|%>
<br>
    <strong><%= link_to jot.title, user_jot_path(jot) %></strong>
		<br>
		<br>
		<%= jot.body%>
		<br>
		<br>
		<%= jot.created_at.strftime(" %A, %B %d - %Y") %>
<%end%>