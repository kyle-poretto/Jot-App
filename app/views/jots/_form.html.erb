<%= form_for(:jot) do |f| %>
    Start Jotting
    <br>
    <br>
  Title<%= f.text_field :title %>
  <br>
  <br>
  <%= hidden_field_tag '[jot][user_id]', current_user.id %>
  Jot
  <br>
  <%= f.text_area :body, size: "60x12" %>
    <%= f.submit %>
<% end %>