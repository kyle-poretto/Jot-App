<% if !current_user.jots.empty? %>
	<strong>Title: </strong>
	<br>
	<%= @jot.title %>
	<br>

	<strong>Body:</strong> <br>

	<%= @jot.body %>
	<br>
	<%= link_to 'Edit Jot', edit_user_jot_path(@jot) %>
	<br>
	<br>

		<% if @jewel.body %>
      <%= @jewel.body %>
			<% else %>
			<%= link_to 'Create Jewel', new_user_jewel_path(@user, @jewel) %>
		<% end %>
		<% else %>
			<%= There are no jots %>
		<% end %>
